# Aether — persistent parallel work marker

## AUTOSTART / IMPORTANT — native AI agent

This file is a persistent project instruction for future Aether OS work.

### Rule
Whenever Aether OS development is being actively performed — debugging, implementing features, fixing build/runtime problems, reviewing code, or preparing commits — also make a small, real, incremental contribution toward a **native Aether AI agent** when the current work allows it.

### Agent requirements
- The agent is a parallel workstream; it must not derail or destabilize the main Aether OS work.
- Prefer native Aether-compatible implementation and architecture over an external hosted assistant or a fake placeholder.
- Build the foundation gradually: interfaces, runtime primitives, memory/state model, local inference/tool abstraction, scheduling/integration, and only then higher-level behavior as the platform permits.
- Every contribution must be real code/design/documentation committed to the project; do not count intentions as progress.
- Do not report every tiny agent change to the user.
- Do not interrupt the main Aether debugging flow with agent progress reports.
- Re-check this marker at the beginning of substantial Aether work so the parallel stream is not forgotten.
- When a usable base exists, surface it to the user for validation.

### Session continuity
This marker exists specifically because conversational sessions and model context can change. It is the project-level reminder and must survive session changes.

### Current state
The native AI-agent workstream has not yet been declared complete or production-ready. Continue building it incrementally alongside Aether OS work.
